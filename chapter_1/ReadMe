第一章简介

1.1 hello,world（一些基本的Go知识点）
    （1）Go编译执行的命令
        go run：将一个或多个以.go为后缀的文件编译，链接，然后运行生成的可执行文件（不保留可执行文件）
            ex. $ go run helloworld.go hellofunc.go
        go build：进行编译，生成二进制程序（编译代码需包含main函数）
        go fmt：格式化代码命令
            ex. $ go fmt helloworld.go
    （2）Go代码使用包来组织。Go规定，在缺失或导入不需要的包情况下，编译会失败
        import 在导入包的时候，会以 $GOPATH/src 目录为根目录
1.2 命令行参数
1.3 找出重复行
    通过 ctrl+d 结束 input.Scan() 的输入
    map里面的键的迭代顺序不固定，通常是随机，每次运行都不一致
    %+v     打印结构体值
    %#v     打印结构体名&值
1.5 获取一个URL
